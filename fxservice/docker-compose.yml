version: '3'
services:
  fxservice:
    build: .
    volumes:
      - /fx/fxservice/log:/logs
      - /opt/projects/fx/fxservice/fxservice:/code/
    stdin_open: true
    ports:
      - "9000:9000"
    environment:
      host: "influxdb"
    links:
      - influxdb
    depends_on:
      - influxdb
    container_name: "data-service"

  influxdb:
    image: influxdb:latest
    container_name: influxdb
    restart: always
    ports:
      - "8083:8083"
      - "8086:8086"
      - "8090:8090"
    env_file:
      - 'env.influxdb'
    volumes:
      - /fx/db:/var/lib/influxdb
